✅ ใช้งาน backend นี้:

1. วางไฟล์ serviceAccountKey.json (Firebase Admin SDK) ไว้ในโฟลเดอร์นี้
2. รันคำสั่ง:
   cd earthquake-backend
   npm install
   npm start

3. ยิง POST ไปที่: http://localhost:3000/notify

